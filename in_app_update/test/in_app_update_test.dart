import 'package:flutter_test/flutter_test.dart';

import 'package:in_app_update/in_app_update.dart';

void main() {
  test('adds one to input values', () {
    expect(InAppUpdate.init(), null);
  });
}
